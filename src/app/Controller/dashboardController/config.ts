export default function () {
    return { //config
        component_type: 0, // 1 table | 0 content
        description:': This module handles IP`s Management, including adding, editing, and deleting users.',
        module_name: 'Dashboard',
    };
}
